<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/container_tablet_bg"
    android:orientation="vertical">
    <FrameLayout
        android:id="@+id/main_holder"
        android:layout_gravity="top"
        android:layout_width="match_parent"
        android:layout_height="@dimen/search_toolbar_height"
        android:elevation="4dp"
        android:background="?colorPrimary">

        <info.santhosh.evlo.ui.main.SearchToolbar
            android:id="@+id/main_toolbar"
            app:title="@string/search_hint"
            app:titleTextAppearance="@style/SearchToolbar_TitleText"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:layout_margin="@dimen/search_toolbar_margin"
            android:elevation="8dp" />
    </FrameLayout>

    <FrameLayout
        android:id="@+id/main_fragment_container"
        android:background="@android:color/white"
        style="@style/container_tablet_constrain"
        tools:background="@color/cardview_shadow_start_color"/>
</LinearLayout>

